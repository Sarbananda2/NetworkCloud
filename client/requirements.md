## Packages
framer-motion | Page transitions, hover effects, and layout animations
date-fns | Formatting timestamps (e.g., "5 minutes ago")
clsx | Conditional class names utility
tailwind-merge | Merging tailwind classes safely

## Notes
Authentication is handled via Replit Auth (use-auth.ts).
API is read-only. No mutations required for devices.
Using Space Grotesk for headers to give a "technical/dashboard" feel.
